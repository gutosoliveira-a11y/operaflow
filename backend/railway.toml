[build]
builder = "nixpacks"
buildCommand = "DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy npm ci && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
